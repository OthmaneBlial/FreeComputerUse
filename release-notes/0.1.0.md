# FreeComputerUse 0.1.0

**Draft for review — not published.** As of 24 September 2026, npm returns 404
for `free-computer-use@0.1.0`; no Git tag or GitHub release exists. Remove this
draft notice and recheck every registry and asset claim before publication.

FreeComputerUse is a local-first browser automation agent. A configured model
plans bounded actions; Playwright performs browser work locally; the agent
checks whether the requested result was reached.

## Included

- CLI, TypeScript library entry point, and a loopback-only local dashboard.
- DOM-first page observation and bounded, schema-validated browser actions.
- Per-origin website approval, separate confirmation for sensitive actions,
  pause/stop controls, and result checks.
- Local run history and compatible semantic workflow replay. A replayed
  workflow can avoid another model call when the page still matches.
- A local MCP server over `stdio` with four bounded tools; site and sensitive
  action approvals remain human decisions through MCP form elicitation.
- API routes for OpenAI-compatible endpoints and Anthropic Messages, plus local
  Codex CLI and Claude Code subscription routes. Subscription routes use the
  installed CLI and do not bypass account or plan limits.
- A synthetic practice lab and a model-free `Extract the table` workflow.

## Verified candidate checks

- The latest full `npm run validate` at commit `aa9500d` passed 106/106 tests,
  build, security scan and `npm audit` with zero vulnerabilities. Commit
  `1edc13d` adds the validation record to documentation only.
- Two clean source archives of `1edc13d` independently built and packed to the
  same 135-file npm tarball (1,144,408 bytes; 1,902,327 unpacked), including
  `docs/ADOPTION.md` and the full validation report. Offline
  installation in a separate project passed `agent --version`, `agent --help`
  and `agent doctor`; the doctor launched system Chrome. The archive excludes
  `.env`, tests, `node_modules` and SQLite databases.
- From the installed dashboard, the documented public synthetic `Extract the
  table` workflow completed after site approval with one local action and zero
  model calls. The verified result contained January 1200/12, February 1800/18
  and March 1500/15. Replaying the learned workflow through the installed CLI
  also completed with one action, zero model calls and one workflow-cache hit.
- The installed MCP CLI connected over `stdio` to the official TypeScript client,
  exposed exactly `start_task`, `inspect_page`, `follow_task` and `stop_task`,
  and rejected a `file://` task URL without a provider. No HTTP endpoint opened.

Candidate tarball SHA-256:

```text
a7c5684b3314b6c1f43236f7d960d3b21ca52c353d7546bbc1ec36700c2a01d4
```

npm integrity:

```text
sha512-XC+Rl8Sj76pOTGnuBusrU/f/VxbcSn9Slv8LxrzIzypwROwA/Rf2/DbXDZTF/oelahITHyyJtZz+OSn5MuxKjQ==
```

## Compatibility limits

- Verified target: macOS 26.6 on Apple Silicon, Node 25.9.0, and system Chrome
  154.0.8037.57. The declared Node floor of 22.13.0, other operating systems,
  browser builds, and standalone binaries are not verified.
- One synthetic DeepSeek Flash completion and one synthetic plan through Codex
  CLI 0.156.1 with a ChatGPT login have live evidence. The ChatGPT check did not
  run a browser task. OpenAI, xAI/Grok, Gemini, Mistral, OpenRouter, and
  Anthropic API routes remain unverified against their live services.
- The installed Claude Code CLI failed its local version command; the project
  requires Claude Code 2.1.248 or newer before attempting that route.
- Run history, profiles, workflows, and downloads stay on disk but are not
  encrypted and do not expire automatically. Selected task and page context
  goes to the configured model provider; screenshots are not sent.

See the [provider evidence](../docs/SUPPORT_MATRIX.md),
[provider setup](../docs/PROVIDERS.md),
[security limits](../SECURITY.md), and [source installation](../README.md).

## Installation status

The public `npm install free-computer-use` command stays omitted until the
registry publishes and verifies this version. The current working installation
guide is the source checkout in the README.

To review the local tarball candidate on the verified macOS target, place
`free-computer-use-0.1.0.tgz` in a new directory and run:

```sh
npm init -y
npm install ./free-computer-use-0.1.0.tgz
FCU_BROWSER_CHANNEL=chrome ./node_modules/.bin/agent doctor
FCU_BROWSER_CHANNEL=chrome ./node_modules/.bin/agent ui
```

This archive is a local release candidate, not a published npm package or a
standalone binary. It requires Node.js and installed Chrome. The local sandbox
workflow, MCP smoke, reproducible tarball checksums and current registry state
are recorded above. The npm registry still returned 404 on 24 September 2026.
